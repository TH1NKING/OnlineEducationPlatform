server {
    listen 80;
    server_name localhost;
    client_max_body_size 100M;

    # 1. 前端静态页面 (Vue/React 构建产物)
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html; # 支持 SPA 路由
    }

    # 2. 后端 API 转发 (负载均衡入口)
    location /api/ {
        proxy_pass http://backend:8080; # 转发给后端容器
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 3. 视频/文件存储代理 (模拟 CDN 回源 S3)
    # 前端访问 http://localhost/video/xxx.mp4 -> Nginx -> MinIO
    location /oss/ {
        proxy_pass http://minio:9000/; # 转发给 MinIO API
        proxy_set_header Host minio:9000;
        
        # 性能优化 (满足“缓冲不超过3秒”)
        proxy_buffering off;      # 关闭缓冲，利于流媒体
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }
}

server {
    listen 443 ssl;  # 监听 443 并开启 SSL
    server_name localhost;
    client_max_body_size 100M;

    # 配置证书路径 (对应你在 docker-compose 里挂载进去的路径)
    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;

    # SSL 优化配置 (推荐加上)
    ssl_session_timeout 5m;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
    ssl_prefer_server_ciphers on;

    # 下面复制你原来的反向代理逻辑
    location /api/ {
        proxy_pass http://backend:8080; # 转发给 Go 后端
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # 👇 告诉后端现在是 HTTPS 协议
        proxy_set_header X-Forwarded-Proto https; 
    }

    # 静态资源
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}